import java.util.EventListener;

/**
 * Represents a general listener of any type of guitar event. Each type of event (and corresponding event listener)
 * should have its own implementation of this interface
 * @author Callum Browne
 */
public interface GuitarStateChangeListener extends EventListener {
}
